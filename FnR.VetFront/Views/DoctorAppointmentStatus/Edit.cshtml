@using System.Web.Optimization
@model FnR.Models.DoctorAppointmentStatus

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_VetLayout.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>DoctorAppointmentStatus</legend>

        @Html.HiddenFor(model => model.Id)
        
        <div class="editor-label">
            @Html.Label("Doctor")
        </div>
        <div class="editor-field">
            @Html.HiddenFor(model => model.DoctorId)
            @*<input type="text" readonly="readonly" id="VetName" value="@Model.Vet.Name"/>*@
            @Model.Doctor.Name
            @*<input type="text" readonly="readonly" value="@Model.Doctor.Name" name="DoctorName" id="DoctorName" class="text-box single-line"/>*@
            @*@Html.ValidationMessageFor(model => model.Doctor.Name)*@
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ExpectedWaitTime)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ExpectedWaitTime)
            @Html.ValidationMessageFor(model => model.ExpectedWaitTime)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.ExpectedDurationOfDelay)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ExpectedDurationOfDelay)
            @Html.ValidationMessageFor(model => model.ExpectedDurationOfDelay)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
